# Activity Log chỉ là text stream (PAGE Workflow)

Không phân loại log

Không biết log thuộc task nào

Không có severity (info/warn/error)

Không support filter

# Pause FUNCTION  (PAGE Workflow)
force pause -> Continue thì start tiếp